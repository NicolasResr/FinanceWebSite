server.port=8080

# --- Database (Aiven MySQL) ---
# A URL foi ajustada para usar o Host e Porta da Aiven com parâmetros de SSL
spring.datasource.url=jdbc:mysql://mysql-2494f079-paleos.i.aivencloud.com:21973/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_V2AZqsPJPyrrlQ2WgFb
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Dialeto atualizado para versões modernas do MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- JWT ---
jwt.secret=3f8b2c1d9e4a7f6b0c5d2e8a1b4f7c9d3e6a2b8f1c4d7e0a9b2c5f8d1e4a7b
jwt.expiration=86400000

# --- CORS ---
# Se o seu FrontEnd estiver usando Vite, a porta padrão costuma ser a 5173
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

# --- Jackson ---
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
